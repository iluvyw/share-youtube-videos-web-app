import { FC } from "react";

const Loading: FC = () => {
  return (
    <div className="fixed top-0 left-0 bg-white z-20 flex flex-col items-center justify-center">
      Loading...
    </div>
  );
};
export default Loading;
